# Property Inspector (decommissioned)

This app has been decommissioned. The URL now redirects to the branded ChiefEO Inspector at https://chiefeotool.com/chiefeoinspector. The branded `chiefeo-inspector` repo is the only live app; this repo is just a static redirect stub with a killswitch service worker (`/sw.js`) that unregisters the old PWA and clears its caches.
